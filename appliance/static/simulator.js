let callId="";const chat=document.getElementById("chat"),form=document.getElementById("sim-form"),input=document.getElementById("sim-input");function add(role,text){const el=document.createElement("div");el.className=`message ${role}`;el.innerHTML=`<strong>${role}</strong><p></p>`;el.querySelector("p").textContent=text;chat.appendChild(el);chat.scrollTop=chat.scrollHeight}async function send(text="",reset=false){const res=await fetch("/api/simulate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({call_uuid:callId,text,reset})});const data=await res.json();callId=data.call_uuid;if(text)add("caller",text);add("assistant",data.reply)}form.addEventListener("submit",e=>{e.preventDefault();const value=input.value.trim();if(!value)return;input.value="";send(value)});document.getElementById("reset").onclick=()=>{chat.innerHTML="";send("",true)};send();
